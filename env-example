# Database Configuration - Centralized for all components
# Set DB_CONNECTION to 'mysql' for MySQL or 'sqlite' for SQLite
DB_CONNECTION="sqlite" # or "mysql"
# SQLite Configuration (when DB_CONNECTION="sqlite")
DATABASE_URL="sqlite:///headless-pm.db"           # Usage tracking database

# MySQL Configuration (when DB_CONNECTION="mysql")
# All components will use the same MySQL instance with different tables
DB_HOST=localhost
DB_PORT=3306
DB_NAME=headless_pm
DB_TEST_NAME=headless_pm_test
DB_USER=root
DB_PASSWORD=*****

# API Security
API_KEY="XXXXXX"
ENVIRONMENT=development

# API Configuration
API_RATE_LIMIT=100
API_RATE_LIMIT_PERIOD=60

SERVICE_PORT=6969
MCP_PORT=6968
SERVICER_HOST=localhost

# this is used by the client process
# Removed API_KEY_HEADLESS_PM - use API_KEY instead
DASHBOARD_PORT=3001
